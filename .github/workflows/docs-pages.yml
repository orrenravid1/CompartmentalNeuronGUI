name: docs-pages

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

concurrency:
  group: docs-pages-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  validate-base:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: |
            pyproject.toml
            poetry.lock

      - name: Install project and docs dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[contrib]"

      - name: Validate docs and generated metadata without backend runtimes
        run: python -m pytest tests/test_docs_and_indexes.py -k "not test_mkdocs_builds_in_strict_mode"

  build-published-docs:
    runs-on: windows-latest
    needs: validate-base
    env:
      NO_MKDOCS_2_WARNING: "true"
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"
          cache-dependency-path: |
            pyproject.toml
            poetry.lock

      - name: Install project, docs, and backend dependencies for published site
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[contrib]"

      - name: Build published docs site
        run: python -m mkdocs build --strict

      - name: Setup Pages
        if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

  deploy:
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    needs:
      - validate-base
      - build-published-docs
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
